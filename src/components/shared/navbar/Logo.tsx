const Logo = () => {
  const logoText = "</> ShipanMallik";
  return (
    <div>
      <h1 className="text-site-gradient font-semibold text-lg">{logoText}</h1>
    </div>
  );
};

export default Logo;
